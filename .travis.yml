language: python
python:
    - 3.4
install:
    - pip install -r requirements.txt
    - pip install coveralls

script: python manage.py runtests --cov-report=term --cov=my_app

after_success:
    - coveralls